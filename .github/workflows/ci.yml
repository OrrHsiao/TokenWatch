name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  XCODE_PROJECT: TokenWatch.xcodeproj
  XCODE_SCHEME: TokenWatch
  XCODE_APP_PRODUCT_NAME: AI Token Watch

jobs:
  debug-build:
    name: macOS Debug Build
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Show Xcode Version
        run: xcodebuild -version

      - name: Build Debug App
        run: |
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Debug \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/TokenWatchDebugDerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            build

  unit-tests:
    name: macOS Unit Tests
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Show Xcode Version
        run: xcodebuild -version

      - name: Run Unit Tests
        env:
          TEST_RUNNER_TOKENWATCH_SKIP_INITIAL_DIRECTORY_AUTHORIZATION_GUIDE: "YES"
        run: |
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -destination 'platform=macOS' \
            -only-testing:TokenWatchTests \
            -skip-testing:TokenWatchUITests \
            -derivedDataPath "$RUNNER_TEMP/TokenWatchDerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            test

  ui-tests:
    name: macOS UI Tests
    # macOS 26 的原生 Liquid Glass 会让 XCTest 将卡片内按钮误判为不可点击；
    # 项目最低支持 macOS 15，固定该 runner 可稳定验证实际交互。
    runs-on: macos-15
    if: github.event_name != 'pull_request'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Show Xcode Version
        run: xcodebuild -version

      - name: Run UI Tests
        run: |
          # UI tests use ad-hoc signing; hosted runners don't have the App Group profile.
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -destination 'platform=macOS' \
            -only-testing:TokenWatchUITests \
            -skip-testing:TokenWatchTests \
            -derivedDataPath "$RUNNER_TEMP/TokenWatchUIDerivedData" \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="" \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGN_ENTITLEMENTS="" \
            test

  release-dry-run:
    name: macOS Release Dry Run
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Show Xcode Version
        run: xcodebuild -version

      - name: Build Release App
        run: |
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/TokenWatchReleaseDerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=NO \
            ARCHS="arm64 x86_64" \
            build

      - name: Verify Release App
        run: |
          APP_PATH="$RUNNER_TEMP/TokenWatchReleaseDerivedData/Build/Products/Release/$XCODE_APP_PRODUCT_NAME.app"
          APP_BINARY="$APP_PATH/Contents/MacOS/$XCODE_APP_PRODUCT_NAME"
          test -d "$APP_PATH"
          test -x "$APP_BINARY"
          ARCHS="$(lipo -archs "$APP_BINARY")"
          echo "Built architectures: $ARCHS"
          case " $ARCHS " in
            *" arm64 "*) ;;
            *) echo "Missing arm64 architecture"; exit 1 ;;
          esac
          case " $ARCHS " in
            *" x86_64 "*) ;;
            *) echo "Missing x86_64 architecture"; exit 1 ;;
          esac
