name: Release

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: GitHub Release tag to create or update
        required: false
        type: string
  push:
    tags:
      - "v*"
  release:
    types:
      - published

permissions:
  contents: write

env:
  XCODE_APP_PRODUCT_NAME: AI Token Watch
  RELEASE_ZIP_NAME: AI-Token-Watch-macOS-universal.zip

jobs:
  build:
    name: macOS Release Build
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Show Xcode Version
        run: xcodebuild -version

      - name: Build Release App
        run: |
          xcodebuild \
            -project TokenWatch.xcodeproj \
            -scheme TokenWatch \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/TokenWatchDerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=NO \
            ARCHS="arm64 x86_64" \
            build

      - name: Verify Universal Binary
        run: |
          APP_BINARY="$RUNNER_TEMP/TokenWatchDerivedData/Build/Products/Release/$XCODE_APP_PRODUCT_NAME.app/Contents/MacOS/$XCODE_APP_PRODUCT_NAME"
          ARCHS="$(lipo -archs "$APP_BINARY")"
          echo "Built architectures: $ARCHS"
          case " $ARCHS " in
            *" arm64 "*) ;;
            *) echo "Missing arm64 architecture"; exit 1 ;;
          esac
          case " $ARCHS " in
            *" x86_64 "*) ;;
            *) echo "Missing x86_64 architecture"; exit 1 ;;
          esac

      - name: Package App
        run: |
          APP_PATH="$RUNNER_TEMP/TokenWatchDerivedData/Build/Products/Release/$XCODE_APP_PRODUCT_NAME.app"
          test -d "$APP_PATH"
          ditto -c -k --keepParent "$APP_PATH" "$RUNNER_TEMP/$RELEASE_ZIP_NAME"

      - name: Upload Release Artifact
        uses: actions/upload-artifact@v7
        with:
          name: AI-Token-Watch-macOS-universal
          path: ${{ runner.temp }}/${{ env.RELEASE_ZIP_NAME }}
          if-no-files-found: error

      - name: Resolve Release Tag
        id: release-tag
        if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '')
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            echo "tag=${{ github.event.release.tag_name }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${{ github.event.inputs.release_tag }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Ensure GitHub Release
        if: github.event_name == 'workflow_dispatch' && steps.release-tag.outputs.tag != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          RELEASE_TAG="${{ steps.release-tag.outputs.tag }}"
          if gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" > /dev/null 2>&1; then
            echo "Release $RELEASE_TAG already exists."
          else
            gh release create "$RELEASE_TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --target "$GITHUB_SHA" \
              --title "$RELEASE_TAG" \
              --notes "Automated release for $RELEASE_TAG."
          fi

      - name: Upload Asset to GitHub Release
        if: steps.release-tag.outputs.tag != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ steps.release-tag.outputs.tag }}" "$RUNNER_TEMP/$RELEASE_ZIP_NAME" --repo "$GITHUB_REPOSITORY" --clobber
